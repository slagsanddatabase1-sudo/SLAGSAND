/**
 * Dummy entry point for Vercel.
 * This file exists to satisfy Vercel's requirement for a main entry point
 * when it misidentifies a static Vite project as a Node.js application.
 */

export default function handler(req, res) {
  res.status(200).json({ status: 'Project build successful' });
}
